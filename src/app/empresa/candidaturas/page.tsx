import { EmConstrucao } from "@/components/layout/em-construcao";

export default function EmpresaCandidaturasPage() {
  return (
    <EmConstrucao
      tipo="empresa"
      titulo="Candidaturas recebidas"
      descricao="A tela de triagem detalhada será implementada na Fase 4 (Handshake)."
    />
  );
}
